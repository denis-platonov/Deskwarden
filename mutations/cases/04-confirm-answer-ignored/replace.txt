                    // [mutant 04] the confirmation is still shown; its answer
                    // is read and discarded.
                    let _ = confirmed_by_preflight(gate, guard, &item, &choice, totp.as_deref());
